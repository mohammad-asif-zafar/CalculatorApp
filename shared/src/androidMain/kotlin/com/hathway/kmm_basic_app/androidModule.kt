/*
import org.koin.androidx.viewmodel.dsl.viewModel
import org.koin.dsl.module

val androidModule = module {
    viewModel { CalculatorViewModel(get()) } // inject HistoryRepository
    viewModel { ItemsViewModel(get(), get()) } // inject CalculationRepo + HistoryRepository
}*/
