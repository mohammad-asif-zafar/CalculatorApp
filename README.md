Turbine if you’re testing Flow.

Happy path → correct output with normal input.
Edge cases → boundary values, limits.
Invalid input / error cases → exceptions, nulls, or weird inputs.
Special cases → inputs that behave differently (negative numbers, decimals, etc.).
